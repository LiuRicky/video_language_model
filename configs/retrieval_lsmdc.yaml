image_root: '[path_to_frames]'
ann_root: 'annotation/lsmdc_retrieval'

# set pretrained as a file path or an url

# size of vit model; base or large
# # pretrained: 'https://storage.googleapis.com/sfr-vision-language-research/BLIP/models/model_base_retrieval_coco.pth'
# vit: 'base'
# batch_size_train: 8
# batch_size_test: 16
# vit_grad_ckpt: False
# vit_ckpt_layer: 0
# init_lr: 1e-5

pretrained: ''
vit: 'large'
batch_size_train: 8
batch_size_test: 16
vit_grad_ckpt: False
vit_ckpt_layer: 0
init_lr: 1e-5

image_size: 224
num_frm: 8
queue_size: 57600
alpha: 0.4
k_test: 128
negative_all_rank: True

prompt: ''

# optimizer
weight_decay: 0.05
min_lr: 0
max_epoch: 6